/**
 * Auditoria Lighthouse objetivo: carga del padron.
 *
 * Ruta objetivo:
 * - `/padron/cargar`
 *
 * Pendiente:
 * - preparar sesion administrativa mockeada o seed.
 * - auditar render de zona de carga y progreso.
 * - validar accessibility del input de archivo y mensajes de resultado.
 * - definir presupuesto para pantalla sin archivo seleccionado.
 */
