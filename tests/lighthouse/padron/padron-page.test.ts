/**
 * Auditoria Lighthouse objetivo: administracion del padron.
 *
 * Ruta objetivo:
 * - `/padron`
 *
 * Pendiente:
 * - preparar sesion administrativa mockeada o seed.
 * - auditar render de filtros, tabla y paginacion.
 * - validar accessibility de busqueda, acciones y estados de carga.
 * - definir presupuesto con un padron seed representativo.
 */
