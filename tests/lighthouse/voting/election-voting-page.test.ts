/**
 * Auditoria Lighthouse objetivo: detalle de votacion.
 *
 * Ruta objetivo:
 * - `/votaciones/:id`
 *
 * Pendiente:
 * - preparar sesion de votante mockeada o seed.
 * - crear eleccion seed estable para reemplazar `:id`.
 * - auditar render de papeleta, opciones y confirmacion.
 * - validar accessibility del flujo de seleccion de voto.
 */
