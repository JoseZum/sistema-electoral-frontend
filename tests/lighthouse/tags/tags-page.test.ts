/**
 * Auditoria Lighthouse objetivo: gestion de etiquetas.
 *
 * Ruta objetivo:
 * - `/tags`
 *
 * Pendiente:
 * - preparar sesion administrativa mockeada o seed.
 * - auditar render de lista, selector y editor de miembros.
 * - validar accessibility de controles de color y asignacion.
 * - definir presupuesto con etiquetas seed.
 */
